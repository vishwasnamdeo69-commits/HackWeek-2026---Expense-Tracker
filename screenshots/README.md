# Screenshots

Add captures of the running application here:

- `dashboard.png` — full dashboard view (hero + summary cards)
- `analytics.png` — the three analytics charts
- `budget.png` — the budget panel with the progress ring
- `expense-management.png` — the expense form and list

These are referenced from the main `README.md`.
